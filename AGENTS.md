# AGENTS.md

## Cursor Cloud specific instructions

This repository ("CursorAgent") is currently an empty/skeleton repo with no application code, dependencies, or services. As of the initial setup (Feb 2026), the repo contains only a `README.md`.

- **No build/lint/test commands** are available — there is no `package.json`, `Makefile`, `requirements.txt`, or equivalent.
- **No services** need to be started.
- **No dependencies** need to be installed.

When application code is added, update this section with the relevant build, lint, test, and run commands, plus any non-obvious caveats.
